name: Deploy dadman-docs

on:
  push:
    branches: [main]
    paths:
      - "dadman-docs/**"
      - ".github/workflows/deploy.yml"

jobs:
  build-and-push:
    name: Build Docker image → GHCR
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write   # required to push to ghcr.io

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./dadman-docs
          push: true
          # Tag with :latest (Watchtower watches this) + immutable SHA tag
          tags: |
            ghcr.io/${{ github.repository_owner }}/dadman-docs:latest
            ghcr.io/${{ github.repository_owner }}/dadman-docs:${{ github.sha }}
          # Build multi-arch so the image runs on both amd64 (TrueNAS) and arm64
          platforms: linux/amd64,linux/arm64
          # Layer cache — makes rebuilds fast on repeated pushes
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Make package public (first push only — idempotent)
        # Ensures Watchtower can pull without registry credentials
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh api \
            --method PATCH \
            -H "Accept: application/vnd.github+json" \
            /user/packages/container/dadman-docs \
            -f visibility=public \
            2>/dev/null || echo "Package visibility unchanged (may not exist yet)"

      - name: Notify Watchtower (optional — triggers immediate pull)
        # Requires WATCHTOWER_HTTP_API_TOKEN secret to be set in repo settings.
        # If the secret is blank/absent, this step is silently skipped.
        if: ${{ secrets.WATCHTOWER_HTTP_API_TOKEN != '' }}
        run: |
          curl -s -f \
            -H "Authorization: Bearer ${{ secrets.WATCHTOWER_HTTP_API_TOKEN }}" \
            "http://10.10.1.24:9000/v1/update" || echo "Watchtower ping skipped"
